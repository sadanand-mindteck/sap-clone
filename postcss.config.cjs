module.exports = {
  plugins: {
    "@tailwindcss/postcss": {},
    // autoprefixer is optional in v4 because vendor prefixing is handled,
    // but leaving it here is fine if you need it for legacy tweaks.
    autoprefixer: {},
  },
};
